# Supercheck - Production with HTTPS (Recommended)
#
# Production-ready deployment with automatic SSL/TLS via Traefik and Let's Encrypt.
# Includes all services: Traefik, PostgreSQL, Redis, MinIO (S3), App, and Worker.
#
# Quick Start:
#   1. Clone the repository
#   2. Configure DNS: app.yourdomain.com → Server IP
#   3. Run ./init-secrets.sh to generate secure secrets
#   4. Run ./setup-k3s.sh to install local K3s + gVisor
#   5. Edit .env with your domain (OAuth optional)
#   6. Run: KUBECONFIG_FILE=/etc/rancher/k3s/supercheck-worker.kubeconfig docker compose -f docker-compose-secure.yml up -d
#   7. Access: https://app.yourdomain.com
#
# Upgrading:
#   Set SUPERCHECK_VERSION in your .env file or shell environment:
#     SUPERCHECK_VERSION=1.4.0 docker compose -f docker-compose-secure.yml up -d
#   This updates app, worker in one place.
#
# Required .env variables:
#   APP_DOMAIN       - Your domain (e.g., app.yourdomain.com)
#   ACME_EMAIL       - Email for Let's Encrypt notifications
#
# IMPORTANT - Let's Encrypt Requirements:
#   - Port 80 must be accessible from the internet (for HTTP-01 challenge)
#   - DNS must be properly configured before starting
#
# IMPORTANT - Cloudflare Users:
#   If using Cloudflare proxy (orange cloud icon), you MUST:
#   1. Set SSL/TLS encryption mode to "Full" or "Full (Strict)" in Cloudflare Dashboard
#   2. The default "Flexible" mode causes redirect loops (ERR_TOO_MANY_REDIRECTS)
#   Go to: Cloudflare Dashboard → SSL/TLS → Overview → Select "Full (strict)"
#
# Data Persistence:
#   PostgreSQL data is stored in a Docker named volume (postgres-data).
#   IMPORTANT: Take regular backups! See Maintenance section in docs.
#   Volume data can be lost if Docker is reinstalled or storage is reset.
#
# Documentation: https://supercheck.io/docs/app/deployment/self-hosted
# ============================================================================

# Project name ensures consistent volume naming across different directories
name: supercheck

# YAML anchors for shared configuration
x-common-env: &common-env # Database Configuration
  DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@postgres:5432/supercheck}
  DB_HOST: ${DB_HOST:-postgres}
  DB_PORT: ${DB_PORT:-5432}
  DB_USER: ${DB_USER:-postgres}
  DB_PASSWORD: ${DB_PASSWORD:-postgres}
  DB_NAME: ${DB_NAME:-supercheck}

  # Redis Configuration
  REDIS_HOST: ${REDIS_HOST:-redis}
  REDIS_PORT: ${REDIS_PORT:-6379}
  REDIS_PASSWORD: ${REDIS_PASSWORD:-supersecure-redis-password-change-this}
  REDIS_URL: ${REDIS_URL:-redis://:supersecure-redis-password-change-this@redis:6379}
  REDIS_TLS_ENABLED: ${REDIS_TLS_ENABLED:-false}
  REDIS_TLS_REJECT_UNAUTHORIZED: ${REDIS_TLS_REJECT_UNAUTHORIZED:-false}

  # App Configuration - all URLs derived from APP_DOMAIN
  # Users only need to set APP_DOMAIN in .env file
  APP_DOMAIN: ${APP_DOMAIN:-demo.supercheck.dev}
  NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-https://${APP_DOMAIN:-demo.supercheck.dev}}
  APP_URL: ${APP_URL:-https://${APP_DOMAIN:-demo.supercheck.dev}}
  STATUS_PAGE_DOMAIN: ${STATUS_PAGE_DOMAIN:-supercheck.dev}
  # Hide the public status page branding footer globally when set to true
  STATUS_PAGE_HIDE_BRANDING: ${STATUS_PAGE_HIDE_BRANDING:-false}
  BETTER_AUTH_URL: ${BETTER_AUTH_URL:-https://${APP_DOMAIN:-demo.supercheck.dev}}
  BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-CHANGE_THIS_GENERATE_32_CHAR_HEX}
  NODE_ENV: ${NODE_ENV:-production}
  SHOW_COMMUNITY_LINKS: ${SHOW_COMMUNITY_LINKS:-true}
  # App-Side Capacity Settings
  # RUNNING_CAPACITY controls how many test runs the App allows into "running" state simultaneously.
  # It is NOT a worker setting — the App reads this to gate submissions before they reach the queue.
  # Keep RUNNING_CAPACITY equal to the total number of worker replicas (WORKER_REPLICAS).
  RUNNING_CAPACITY: ${RUNNING_CAPACITY:-1} # App-side gate: max concurrent test runs (keep equal to WORKER_REPLICAS)
  QUEUED_CAPACITY: ${QUEUED_CAPACITY:-10} # App-side gate: max runs allowed to queue before new submissions are rejected
  # Container limits for 2 vCPU / 4GB servers
  CONTAINER_CPU_LIMIT: ${CONTAINER_CPU_LIMIT:-1.5}
  CONTAINER_MEMORY_LIMIT_MB: ${CONTAINER_MEMORY_LIMIT_MB:-2048}
  TEST_EXECUTION_TIMEOUT_MS: ${TEST_EXECUTION_TIMEOUT_MS:-300000}
  JOB_EXECUTION_TIMEOUT_MS: ${JOB_EXECUTION_TIMEOUT_MS:-3600000}
  K6_TEST_EXECUTION_TIMEOUT_MS: ${K6_TEST_EXECUTION_TIMEOUT_MS:-3600000}
  K6_JOB_EXECUTION_TIMEOUT_MS: ${K6_JOB_EXECUTION_TIMEOUT_MS:-3600000}

  # AWS S3 / MinIO Configuration
  AWS_REGION: ${AWS_REGION:-us-east-1}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-minioadmin}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-minioadmin}
  S3_ENDPOINT: ${S3_ENDPOINT:-http://minio:9000}
  # Playwright buckets
  S3_TEST_BUCKET_NAME: ${S3_TEST_BUCKET_NAME:-playwright-test-artifacts}
  S3_JOB_BUCKET_NAME: ${S3_JOB_BUCKET_NAME:-playwright-job-artifacts}
  S3_MONITOR_BUCKET_NAME: ${S3_MONITOR_BUCKET_NAME:-playwright-monitor-artifacts}
  # K6 buckets
  S3_K6_TEST_BUCKET_NAME: ${S3_K6_TEST_BUCKET_NAME:-k6-test-artifacts}
  S3_K6_JOB_BUCKET_NAME: ${S3_K6_JOB_BUCKET_NAME:-k6-job-artifacts}
  # Status page bucket
  S3_STATUS_BUCKET_NAME: ${S3_STATUS_BUCKET_NAME:-status-page-artifacts}
  # Requirements bucket
  S3_REQUIREMENTS_BUCKET_NAME: ${S3_REQUIREMENTS_BUCKET_NAME:-test-requirement-artifacts}
  S3_FORCE_PATH_STYLE: ${S3_FORCE_PATH_STYLE:-true}
  S3_OPERATION_TIMEOUT: ${S3_OPERATION_TIMEOUT:-10000}
  S3_MAX_RETRIES: ${S3_MAX_RETRIES:-3}

  # Notification Channel Limits
  # Notification channel limits (runtime configurable)
  MAX_JOB_NOTIFICATION_CHANNELS: ${MAX_JOB_NOTIFICATION_CHANNELS:-10}
  MAX_MONITOR_NOTIFICATION_CHANNELS: ${MAX_MONITOR_NOTIFICATION_CHANNELS:-10}

  # Document Upload Limits (Requirements System)
  MAX_DOCUMENT_SIZE_MB: ${MAX_DOCUMENT_SIZE_MB:-10}
  MAX_DOCUMENTS_PER_PROJECT: ${MAX_DOCUMENTS_PER_PROJECT:-100}

  # Playwright Configuration - Optimized for Supercheck execution service
  # PLAYWRIGHT_HEADLESS: ${PLAYWRIGHT_HEADLESS:-true}
  PLAYWRIGHT_WORKERS: ${PLAYWRIGHT_WORKERS:-1} # Single worker for 2GB container memory
  PLAYWRIGHT_RETRIES: ${PLAYWRIGHT_RETRIES:-1}
  PLAYWRIGHT_TRACE: ${PLAYWRIGHT_TRACE:-retain-on-failure}
  PLAYWRIGHT_SCREENSHOT: ${PLAYWRIGHT_SCREENSHOT:-on}
  # Video recording enabled on test failures for debugging (with increased resource limits)
  PLAYWRIGHT_VIDEO: ${PLAYWRIGHT_VIDEO:-retain-on-failure}

  # Monitor Results Cleanup (retention per plan from database)
  MONITOR_CLEANUP_ENABLED: ${MONITOR_CLEANUP_ENABLED:-true}
  MONITOR_CLEANUP_CRON: ${MONITOR_CLEANUP_CRON:-"0 2 * * *"} # 2 AM daily
  MONITOR_CLEANUP_BATCH_SIZE: ${MONITOR_CLEANUP_BATCH_SIZE:-1000}
  MONITOR_PRESERVE_STATUS_CHANGES: ${MONITOR_PRESERVE_STATUS_CHANGES:-true}
  MONITOR_CLEANUP_SAFETY_LIMIT: ${MONITOR_CLEANUP_SAFETY_LIMIT:-1000000}

  # Monitor Aggregates Cleanup (retention per plan from database)
  MONITOR_AGGREGATES_CLEANUP_ENABLED: ${MONITOR_AGGREGATES_CLEANUP_ENABLED:-true}
  MONITOR_AGGREGATES_CLEANUP_CRON: ${MONITOR_AGGREGATES_CLEANUP_CRON:-"30 2 * * *"} # 2:30 AM daily
  MONITOR_AGGREGATES_CLEANUP_BATCH_SIZE: ${MONITOR_AGGREGATES_CLEANUP_BATCH_SIZE:-1000}
  MONITOR_AGGREGATES_CLEANUP_SAFETY_LIMIT: ${MONITOR_AGGREGATES_CLEANUP_SAFETY_LIMIT:-500000}

  # Job Runs Cleanup (retention per plan from database)
  JOB_RUNS_CLEANUP_ENABLED: ${JOB_RUNS_CLEANUP_ENABLED:-true}
  JOB_RUNS_CLEANUP_CRON: ${JOB_RUNS_CLEANUP_CRON:-"0 3 * * *"} # 3 AM daily
  JOB_RUNS_CLEANUP_BATCH_SIZE: ${JOB_RUNS_CLEANUP_BATCH_SIZE:-100}
  JOB_RUNS_CLEANUP_SAFETY_LIMIT: ${JOB_RUNS_CLEANUP_SAFETY_LIMIT:-10000}

  # Playground Artifacts Cleanup
  PLAYGROUND_CLEANUP_ENABLED: ${PLAYGROUND_CLEANUP_ENABLED:-true}
  PLAYGROUND_CLEANUP_CRON: ${PLAYGROUND_CLEANUP_CRON:-"0 5 * * *"} # 5 AM daily
  PLAYGROUND_CLEANUP_MAX_AGE_HOURS: ${PLAYGROUND_CLEANUP_MAX_AGE_HOURS:-24}

  # Webhook Idempotency Cleanup (TTL-based deduplication records)
  WEBHOOK_CLEANUP_ENABLED: ${WEBHOOK_CLEANUP_ENABLED:-true}
  WEBHOOK_CLEANUP_CRON: ${WEBHOOK_CLEANUP_CRON:-"0 4 * * *"} # 4 AM daily
  WEBHOOK_CLEANUP_BATCH_SIZE: ${WEBHOOK_CLEANUP_BATCH_SIZE:-1000}
  WEBHOOK_CLEANUP_SAFETY_LIMIT: ${WEBHOOK_CLEANUP_SAFETY_LIMIT:-100000}

  # Security Configuration
  SECRET_ENCRYPTION_KEY: ${SECRET_ENCRYPTION_KEY:-CHANGE_THIS_GENERATE_32_CHAR_HEX}

  # Better Auth Admin Configuration
  MAX_PROJECTS_PER_ORG: ${MAX_PROJECTS_PER_ORG:-10}
  DEFAULT_PROJECT_NAME: ${DEFAULT_PROJECT_NAME:-Default Project}

  # SMTP Email Configuration for Notifications
  # Supports any SMTP provider including Resend SMTP, Gmail, SendGrid, etc.
  SMTP_HOST: ${SMTP_HOST:-}
  SMTP_PORT: ${SMTP_PORT:-587}
  SMTP_USER: ${SMTP_USER:-}
  SMTP_PASSWORD: ${SMTP_PASSWORD:-}
  SMTP_SECURE: ${SMTP_SECURE:-false}
  SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-}

  # ==========================================================================
  # AI Features Configuration (AI Fix, AI Create, AI Analyze)
  # ==========================================================================
  # Supercheck supports multiple AI providers. Choose ONE provider and configure
  # its credentials below. The AI_PROVIDER setting determines which provider is used.
  # --------------------------------------------------------------------------
  # AI Model Configuration
  # Set AI_PROVIDER to one of: openai, azure, anthropic, gemini, google-vertex, bedrock, openrouter
  # --------------------------------------------------------------------------
  AI_PROVIDER: ${AI_PROVIDER:-openai}
  AI_MODEL: ${AI_MODEL:-gpt-4o-mini}
  AI_TIMEOUT_MS: ${AI_TIMEOUT_MS:-90000}
  AI_MAX_RETRIES: ${AI_MAX_RETRIES:-2}
  AI_TEMPERATURE: ${AI_TEMPERATURE:-0.1}

  # --------------------------------------------------------------------------
  # Provider A: OpenAI (default)
  # Get your API key from: https://platform.openai.com/api-keys
  # --------------------------------------------------------------------------
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}

  # --------------------------------------------------------------------------
  # Provider B: Azure OpenAI
  # Setup: https://portal.azure.com → Create Azure OpenAI resource
  # Get keys from: Azure Portal → Your Resource → Keys and Endpoint
  # --------------------------------------------------------------------------
  AZURE_RESOURCE_NAME: ${AZURE_RESOURCE_NAME:-}
  AZURE_API_KEY: ${AZURE_API_KEY:-}
  AZURE_OPENAI_DEPLOYMENT: ${AZURE_OPENAI_DEPLOYMENT:-}
  AZURE_API_VERSION: ${AZURE_API_VERSION:-2024-06-01}
  AZURE_USE_MANAGED_IDENTITY: ${AZURE_USE_MANAGED_IDENTITY:-false}

  # --------------------------------------------------------------------------
  # Provider C: Anthropic Claude
  # Get your API key from: https://console.anthropic.com/settings/keys
  # --------------------------------------------------------------------------
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}

  # --------------------------------------------------------------------------
  # Provider D: Google Gemini (AI Studio - simple API key, recommended)
  # Get your API key from: https://aistudio.google.com/apikey
  # --------------------------------------------------------------------------
  GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY:-}

  # --------------------------------------------------------------------------
  # Provider E: Google Vertex AI (GCP enterprise - requires GCP project)
  # Setup: https://console.cloud.google.com/vertex-ai
  # Requires: GCP project + ADC authentication
  # --------------------------------------------------------------------------
  GOOGLE_VERTEX_PROJECT: ${GOOGLE_VERTEX_PROJECT:-}
  GOOGLE_VERTEX_LOCATION: ${GOOGLE_VERTEX_LOCATION:-us-central1}

  # --------------------------------------------------------------------------
  # Provider F: AWS Bedrock
  # Setup: https://console.aws.amazon.com/bedrock
  # Uses BEDROCK_* prefix to avoid conflicts with S3 credentials
  # On AWS infrastructure: leave credentials empty to use IAM role
  # --------------------------------------------------------------------------
  BEDROCK_AWS_REGION: ${BEDROCK_AWS_REGION:-}
  BEDROCK_AWS_ACCESS_KEY_ID: ${BEDROCK_AWS_ACCESS_KEY_ID:-}
  BEDROCK_AWS_SECRET_ACCESS_KEY: ${BEDROCK_AWS_SECRET_ACCESS_KEY:-}
  BEDROCK_AWS_SESSION_TOKEN: ${BEDROCK_AWS_SESSION_TOKEN:-}

  # --------------------------------------------------------------------------
  # Provider G: OpenRouter (unified gateway to 400+ models)
  # Get your API key from: https://openrouter.ai/keys
  # Model IDs use provider/model format, e.g. anthropic/claude-3.5-haiku
  # --------------------------------------------------------------------------
  OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}

  # Worker Location for geo-distributed monitoring and load testing.
  # Use "local" for single-server self-hosted mode, or any enabled
  # Super Admin location code for region-specific workers.
  WORKER_LOCATION: ${WORKER_LOCATION:-local}

  # ACME/Let's Encrypt email for SSL certificates
  ACME_EMAIL: ${ACME_EMAIL:-admin@example.com}

  # Self-Hosted Mode (set to 'true' for unlimited features)
  # Client-side code fetches this from /api/config/hosting-mode at runtime
  SELF_HOSTED: ${SELF_HOSTED:-true}
  # Registration Controls
  # SIGNUP_ENABLED=false disables open registration (invited users can still register via invitation links)
  # ALLOWED_EMAIL_DOMAINS restricts signup to specific email domains, e.g. 'acme.com,acme.org' (empty = allow all)
  SIGNUP_ENABLED: ${SIGNUP_ENABLED:-true}
  ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}

  # OAuth Configuration (optional - enable social login)
  # Social auth buttons are automatically shown when client ID and secret are configured
  GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:-}
  GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:-}

  GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
  GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}

  # Queue Alerting Configuration (optional)
  QUEUE_ALERTING_ENABLED: ${QUEUE_ALERTING_ENABLED:-true}
  # Alerts: High depth (>70%), Long wait (>15m), High failure (>5%), Stall detection
  QUEUE_ALERT_SLACK_WEBHOOK_URL: ${QUEUE_ALERT_SLACK_WEBHOOK_URL:-}
  QUEUE_ALERT_WEBHOOK_URL: ${QUEUE_ALERT_WEBHOOK_URL:-} # JSON payload { type: 'queue_alert', alert: ... }

services:
  # Traefik Reverse Proxy with HTTPS
  traefik:
    image: traefik:v3.6.6
    environment:
      # Traefik Configuration using environment variables
      - TRAEFIK_API_DASHBOARD=false
      - TRAEFIK_PROVIDERS_DOCKER=true
      - TRAEFIK_PROVIDERS_DOCKER_EXPOSEDBYDEFAULT=false
      - TRAEFIK_ENTRYPOINTS_WEB_ADDRESS=:80
      - TRAEFIK_ENTRYPOINTS_WEBSECURE_ADDRESS=:443
      # Let's Encrypt ACME configuration for automatic SSL certificates
      - TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_HTTPCHALLENGE=true
      - TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_HTTPCHALLENGE_ENTRYPOINT=web
      - TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_EMAIL=${ACME_EMAIL:-admin@example.com}
      - TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_STORAGE=/letsencrypt/acme.json
      - TRAEFIK_LOG_LEVEL=INFO
      - TRAEFIK_ACCESSLOG=true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-letsencrypt:/letsencrypt
    labels:
      - "traefik.enable=false"
    networks:
      - supercheck-network
    restart: unless-stopped

  # App (Next.js Frontend) - Handles migrations internally
  app:
    image: ghcr.io/supercheck-io/supercheck/app:${SUPERCHECK_VERSION:-1.3.3}
    expose:
      - "3000"
    environment:
      <<: *common-env
      # App-specific configuration (inherits all common env vars)
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s # Increased to allow for migrations
    labels:
      - "traefik.enable=true"

      # Main app — uses APP_DOMAIN from environment
      - "traefik.http.routers.app.rule=Host(`${APP_DOMAIN:-demo.supercheck.dev}`)"
      - "traefik.http.routers.app.priority=100"
      - "traefik.http.routers.app.entrypoints=websecure"
      - "traefik.http.routers.app.tls.certresolver=letsencrypt"
      - "traefik.http.routers.app.service=app"

      # Status pages — wildcard subdomain (extracts base domain from APP_DOMAIN)
      # Note: For status pages, configure STATUS_PAGE_DOMAIN in environment
      - "traefik.http.routers.status-pages.rule=HostRegexp(`[a-zA-Z0-9-]+\\.${STATUS_PAGE_DOMAIN:-supercheck.dev}`)"
      - "traefik.http.routers.status-pages.priority=50"
      - "traefik.http.routers.status-pages.entrypoints=websecure"
      - "traefik.http.routers.status-pages.tls.certresolver=letsencrypt"
      - "traefik.http.routers.status-pages.service=app"

      # HTTP → HTTPS redirect for main app
      - "traefik.http.routers.app-http.rule=Host(`${APP_DOMAIN:-demo.supercheck.dev}`)"
      - "traefik.http.routers.app-http.entrypoints=web"
      - "traefik.http.routers.app-http.middlewares=app-https-redirect"
      - "traefik.http.routers.app-http.service=app"

      # HTTP → HTTPS redirect for status pages
      - "traefik.http.routers.status-pages-http.rule=HostRegexp(`[a-zA-Z0-9-]+\\.${STATUS_PAGE_DOMAIN:-supercheck.dev}`)"
      - "traefik.http.routers.status-pages-http.entrypoints=web"
      - "traefik.http.routers.status-pages-http.middlewares=app-https-redirect"
      - "traefik.http.routers.status-pages-http.service=app"

      # Redirect middleware & service config
      - "traefik.http.middlewares.app-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.services.app.loadbalancer.server.port=3000"
      - "traefik.http.services.app.loadbalancer.healthCheck.path=/api/health"
    networks:
      - supercheck-network
    deploy:
      # Scale app instances for better performance (load balanced by Traefik)
      replicas: ${APP_REPLICAS:-2}
      resources:
        limits:
          cpus: "1.5"
          memory: 3G
        reservations:
          cpus: "0.5"
          memory: 1G

  # Worker (NestJS Runner)
  # Self-hosted production uses the Kubernetes execution backend so Docker
  # Compose and cloud deployments share the same per-run gVisor Job model.
  worker:
    image: ghcr.io/supercheck-io/supercheck/worker:${SUPERCHECK_VERSION:-1.3.3}
    # init: Use tini as PID 1 to avoid zombie processes (Playwright recommendation)
    init: true
    # GVISOR-006: Run as non-root user (pwuser UID 1000 from Playwright image)
    user: "1000:1000"
    environment:
      <<: *common-env
      # Worker-specific configuration (inherits all common env vars)
      WORKER_IMAGE: ghcr.io/supercheck-io/supercheck/worker:${SUPERCHECK_VERSION:-1.3.3}
      EXECUTION_NAMESPACE: ${EXECUTION_NAMESPACE:-supercheck-execution}
      KUBECONFIG: /run/secrets/supercheck-kubeconfig
    # GVISOR-006: Read-only root filesystem with writable tmpfs mounts
    read_only: true
    tmpfs:
      - /tmp:size=2G,uid=1000,gid=1000,mode=1777
      - /home/pwuser/.cache:size=256M,uid=1000,gid=1000,mode=0700
      - /home/pwuser/.npm:size=256M,uid=1000,gid=1000,mode=0700
    volumes:
      - worker-playwright-reports:/worker/playwright-reports
      - worker-k6-reports:/worker/k6-reports
      - worker-reports:/worker/report
      - ${KUBECONFIG_FILE:-/etc/rancher/k3s/supercheck-worker.kubeconfig}:/run/secrets/supercheck-kubeconfig:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - supercheck-network
    deploy:
      replicas: ${WORKER_REPLICAS:-1} # Default: 1 worker for self-hosted (scale with WORKER_REPLICAS)
      resources:
        # Resource limits for 2 vCPU / 4GB servers
        # Worker spawns containers with 1.5 CPU + 2GB RAM
        limits:
          cpus: "1.8"
          memory: 3G
          pids: 512
        reservations:
          cpus: "0.5"
          memory: 1G

      # Container Restart Protection (handles stuck/crashed containers)
      restart_policy:
        condition: on-failure
        max_attempts: 3
        delay: 15s
        window: 120s

    # GVISOR-006: Hardened security profile
    security_opt:
      - no-new-privileges:true # Prevent privilege escalation
    cap_drop:
      - ALL
    # Note: gVisor runtime provides kernel-level syscall interception
    # All child processes inherit the sandbox automatically

  # PostgreSQL Database
  postgres:
    image: postgres:18
    environment:
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
      - POSTGRES_DB=${DB_NAME:-supercheck}
    volumes:
      - postgres-data:/var/lib/postgresql
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    networks:
      - supercheck-network
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1.5G
        reservations:
          cpus: "0.5"
          memory: 1G

  # Redis (for job queues)
  redis:
    image: redis:8
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:-supersecure-redis-password-change-this}
    command: sh -c "rm -rf /data/* && redis-server --requirepass \"${REDIS_PASSWORD:-supersecure-redis-password-change-this}\" --maxmemory 512mb --maxmemory-policy noeviction --save '' --appendonly no --protected-mode yes --bind 0.0.0.0 --tcp-keepalive 300"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "redis-cli",
          "-a",
          "${REDIS_PASSWORD:-supersecure-redis-password-change-this}",
          "ping",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - supercheck-network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M

  # MinIO (S3-compatible storage)
  minio:
    image: minio/minio:latest
    environment:
      - MINIO_ROOT_USER=${AWS_ACCESS_KEY_ID:-minioadmin}
      - MINIO_ROOT_PASSWORD=${AWS_SECRET_ACCESS_KEY:-minioadmin}
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - supercheck-network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 1G
        reservations:
          cpus: "0.25"
          memory: 512M

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
  minio-data:
    driver: local
  worker-playwright-reports:
    driver: local
  worker-k6-reports:
    driver: local
  worker-reports:
    driver: local
  traefik-letsencrypt:
    driver: local

networks:
  supercheck-network:
    driver: bridge
